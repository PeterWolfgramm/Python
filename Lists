''' 
Output: Kevin 
        Toby 
        Karen, Jim, Oscar, Toby 
        Karen, Jim 
''' 

friends = ["Kevin", "Karen", "Jim", "Oscar", "Toby"] 

print(friends[0])   # This will display the value at index position 0 – Kevin.  
print(friends[-1])   # This will display the value at index position -1 – Toby.  
print(friends[1:])   # This will display each value starting with index position 1: Karen, Jim, Oscar, Toby.  
print(friends[1:3])   # This will display each value from index position 1 to 3 (not including 3): Karen, Jim.
