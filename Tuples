# Example 1:  
# Output: 4 

coordinates = (4, 5) 

print(coordinates[0])   # This will access the value at index position 0.  
 

# Example 2:  
# Output: 3 

coordinates = [(1, 2), (3, 4), (5, 6)]   # Multiple sets of values can be stored within a tuple.  

print(coordinates[1][0])   # To access a specific value, type [][].  
                           # In the first [] type the index position of the set the value is in.  
                           # In the second [] type the index position of the value within the set.  
